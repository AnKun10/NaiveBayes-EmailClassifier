# NaiveBayes-EmailClassifier
Before run the program, remember to install nltk packages in Python Console:
```
>>> import nltk
>>> nltk.download("stopwords")
>>> nltk.download("punkt")
>>> nltk.download("punkt_tab")
```